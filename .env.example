# AgentLedger Environment Variables

# Required: Base58-encoded Solana private key OR JSON array format (Solana CLI)
# Generate a new key: solana-keygen new --no-bip39-passphrase --outfile /tmp/agent.json
# Convert to base58: cat /tmp/agent.json | node -e "const bs58=require('bs58');const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));console.log(bs58.encode(Buffer.from(d)))"
AGENT_PRIVATE_KEY=

# Agent identifier (used in memo payloads)
AGENT_ID=my-agent-v1

# Helius API key (optional but recommended for faster querying)
# Get one at: https://helius.dev
HELIUS_API_KEY=

# Network: "devnet" or "mainnet-beta" (default: devnet)
SOLANA_NETWORK=devnet

# Override RPC URL (optional — auto-configured from HELIUS_API_KEY if provided)
# SOLANA_RPC_URL=https://api.devnet.solana.com
